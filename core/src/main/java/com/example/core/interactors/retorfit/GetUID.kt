package com.example.core.interactors.retorfit

import com.example.core.data.HabitRepository

class GetUID ( private val habitRepository: HabitRepository) {
    //suspend operator fun invoke() = habitRepository.getUid()
}